server:
  port: 8080

spring:
  application:
    name: gateway

cors:
  allowed-origin: ${CORS_ALLOWED_ORIGIN:http://localhost:5173}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
